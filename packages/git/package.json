{
	"type": "module",
	"sideEffects": false,
	"name": "@ai-toolkit/git",

	"imports": {
		"#lib/*": "./src/lib/*"
	},

	"exports": {
		"./schema": "./src/schema.ts",
		"./service": "./src/service.ts"
	},

	"scripts": {
		"fix": "oxlint --fix-dangerously --silent . || true; oxfmt . || true",
		"check": "oxfmt --check . && oxlint ."
	}
}
