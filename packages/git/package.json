{
	"type": "module",
	"sideEffects": false,
	"name": "@ai-toolkit/git",

	"exports": {
		"./schema": "./src/schema.ts",
		"./service": "./src/service.ts"
	},

	"scripts": {
		"fix": "biome check --write --unsafe",
		"check": "biome check && tsgo --noEmit && effect-language-service diagnostics --project ./tsconfig.json"
	}
}
