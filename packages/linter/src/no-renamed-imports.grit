engine biome(1.0)
language js(typescript, jsx)

or {
	`import { $specifiers } from $_`,
	`import type { $specifiers } from $_`
} where {
	$specifiers <: contains `$name as $alias` as $match where {
		register_diagnostic(span=$match, message="Don't rename imports; use the original exported name")
	}
}
