engine biome(1.0)
language js(typescript, jsx)

`typeof $expr` as $match where {
	register_diagnostic(span=$match, message="Use Predicate helpers: `Predicate.isString(x)`, `Predicate.isNumber(x)`, `Predicate.isObject(x)`.")
}
