engine biome(1.0)
language js(typescript, jsx)

`typeof $expr` as $match where {
	register_diagnostic(span=$match, message="Use Predicate functions instead of typeof; use Predicate.isString, Predicate.isNumber, Predicate.isObject, Predicate.isArray, etc.")
}
