engine biome(1.0)
language js(typescript, jsx)

`$expr as $type` as $match where {
	not $match <: `$_ as const`,
	not $filename <: r".*/components/ui/.*",
	register_diagnostic(span=$match, message="Remove `as` assertion. Inline, simplify, rewrite until inference works.")
}
