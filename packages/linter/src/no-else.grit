engine biome(1.0)
language js(typescript, jsx)

`if ($cond) { $body } else { $elseBody }` as $match where {
	register_diagnostic(span=$match, message="Never use `else`. Rewrite with early returns, `switch` for discriminators, or `Match` for value-producing branches so control flow stays flat")
}
