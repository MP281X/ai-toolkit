engine biome(1.0)
language js(typescript, jsx)

`$value.pipe($args)` as $match where {
	register_diagnostic(span=$match, message="Do not use the `.pipe()` method. Use the free `pipe(value, ...)` function instead")
}
