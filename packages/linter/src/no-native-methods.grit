engine biome(1.0)
language js(typescript, jsx)

`$obj.$method($...)` as $match where {
	$method <: or {
		`map`,
		`filter`,
		`reduce`,
		`reduceRight`,
		`find`,
		`findIndex`,
		`findLast`,
		`findLastIndex`,
		`some`,
		`every`,
		`forEach`,
		`flatMap`,
		`flat`,
		`trim`,
		`trimStart`,
		`trimEnd`,
		`toUpperCase`,
		`toLowerCase`,
		`split`,
		`slice`,
		`substring`,
		`startsWith`,
		`endsWith`,
		`includes`,
		`indexOf`,
		`lastIndexOf`,
		`replace`,
		`replaceAll`,
		`match`,
		`matchAll`,
		`search`,
		`repeat`,
		`padStart`,
		`padEnd`,
		`concat`,
		`charAt`,
		`charCodeAt`,
		`codePointAt`,
		`normalize`
	},
	not $obj <: or {
		`Effect`,
		`Stream`,
		`Layer`,
		`Record`,
		`Option`,
		`Either`,
		`Exit`,
		`Fiber`,
		`Chunk`,
		`Schedule`,
		`Duration`,
		`Cause`,
		`Match`,
		`Schema`,
		`Array`,
		`String`
	},
	register_diagnostic(span=$match, message="Do not use native prototype methods here. Use the Effect modules instead: `Array.*` for arrays, `String.*` for strings, and `Record.*` for object-like data. Suppress this rule with `// biome-ignore lint/plugin: not a prototype method` only when calling external API methods that happen to share the same name as JS prototype methods")
}
