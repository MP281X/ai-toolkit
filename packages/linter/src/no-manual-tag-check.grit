engine biome(1.0)
language js(typescript, jsx)

`$expr._tag === $tag` as $match where {
	register_diagnostic(span=$match, message="Use Effect module helpers instead of manual _tag checks; use Option.isSome/isNone for Option, Match.is for union tags, or Predicate.isTagged for custom tagged types")
}
