engine biome(1.0)
language js(typescript, jsx)

or {
	`return undefined`,
	`return null`
} as $match where {
	register_diagnostic(span=$match, message="Do not return `undefined` or `null` explicitly here. Use bare `return` instead; the explicit value is meaningless noise")
}
