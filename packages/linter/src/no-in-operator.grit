engine biome(1.0)
language js(typescript, jsx)

`'$prop' in $expr` as $match where {
	register_diagnostic(span=$match, message="Do not use the `in` operator. Use `Predicate.hasProperty(object, 'prop')` or `Predicate.hasProperty('prop')(object)` instead")
}
