{
	"type": "module",
	"sideEffects": false,
	"name": "@ai-toolkit/linter",

	"scripts": {
		"fix": "biome check --write --unsafe --vcs-root=../.. --config-path=biome.refactor.json || true",
		"check": "tsgo --noEmit && biome check --vcs-root=../.. --config-path=biome.refactor.json"
	}
}
