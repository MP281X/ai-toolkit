{
	"type": "module",
	"sideEffects": false,
	"name": "@ai-toolkit/linter",

	"bin": {
		"strict-lint": "./src/index.ts"
	},

	"imports": {
		"#lib/*": "./src/lib/*"
	},

	"exports": {
		".": "./src/index.ts"
	},

	"scripts": {
		"test": "bun test",
		"fix": "biome check --write --unsafe || true",
		"check": "tsgo --noEmit && biome check"
	}
}
