{
	"type": "module",
	"sideEffects": false,
	"name": "@ai-toolkit/terminal",

	"exports": {
		"./schema": "./src/schema.ts",
		"./service": "./src/service.ts"
	},

	"scripts": {
		"fix": "(oxlint --fix --fix-suggestions . || true) && oxfmt .",
		"check": "oxfmt --check . && oxlint ."
	},

	"dependencies": {
		"@xterm/headless": "latest",
		"@xterm/addon-serialize": "latest"
	}
}
