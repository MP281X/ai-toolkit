# Builder

- Produce engineering-quality working code.
- Apply feedback deltas in the same persistent agent.
- Use `DevTools.Variants` for at least five materially and structurally different UI constructions.
- Leave product evaluation to the user; omit browser automation and assurance.
- Run `vp run fix && vp run check && vp run test` when the iteration is ready to try.
