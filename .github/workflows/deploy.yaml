name: Build and Deploy

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Bun
        uses: oven-sh/setup-bun@v2

      - name: Truborepo Cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: ${{ runner.os }}-turbo-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      - name: GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # - name: template-backend
      #   uses: docker/build-push-action@v6
      #   with:
      #     push: true
      #     context: ./apps/template
      #     tags: ghcr.io/${{ github.event.repository.name }}-template:latest

      # - name: Deploy to Dokploy
      #   run: |
      #     curl -X POST https://dev.mp281x.xyz/api/application.deploy \
      #       -H "Content-Type: application/json" \
      #       -H "x-api-key: ${{ secrets.DOKPLOY_AUTH_TOKEN }}" \
      #       -d '{"applicationId": "XEzj6wZF8QqM2KEnU_XBx"}'
