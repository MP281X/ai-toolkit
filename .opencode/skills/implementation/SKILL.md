---
name: implementation
description: 'Use for workspace edits and validation.'
---

1. Strictly follow `AGENTS.md` and every loaded skill.
2. Resolve the assigned Owners and direct Coupled paths.
3. Before editing, read the current target range when exact current content is not established or the file changed since its last read. Do not reread unaffected evidence.
4. Apply the approved Construction without redesign.
5. Preserve unrelated work.
6. Run the exact validation selected by `AGENTS.md`.
7. Correct implementation and validation failures within the Contract.
8. Return a blocker when correction requires a design change.
